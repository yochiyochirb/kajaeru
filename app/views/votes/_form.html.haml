.row
  .col-md-12
    投票したいメンバーを選んでください。

    = form_for(@vote, url: polymorphic_path([@event, @vote]), html: {class: 'candidates'}) do |f|
      %ul
        - @candidates.each do |candidate|
          %li.radio
            = f.radio_button :candidate_id, candidate.id
            = image_tag candidate.member.image, alt: candidate.member.nickname, size: '20x20', class: 'img-rounded'
            = f.label :candidate_id, candidate.member.nickname, value: candidate.id
      .form-group
        = f.label :comment, '推薦コメント(任意)', class: 'control-label'
        .comment
          = f.text_area :comment, class: 'form-control', rows: 3
      .form-group
        = f.submit button_label, class: 'btn btn-default'
