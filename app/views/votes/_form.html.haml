%div
  投票したいメンバーを選んでください。

= form_for(@vote, html: {class: 'candidates'}) do |f|
  %ul
    - @candidates.each do |candidate|
      %li
        = f.radio_button :candidate_id, candidate.id
        = image_tag candidate.image, alt: candidate.nickname, size: '40x40'
        = f.label :candidate_id, candidate.nickname, value: candidate.id
  %div
    = f.label :comment, '推薦コメント(任意)'
  %div
    .comment
      = f.text_area :comment
  = f.submit button_label
