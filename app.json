{
  "name": "kajaeru",
  "description": "",
  "scripts": {
    "postdeploy": "cp ./config/database.yml.sample ./config/database.yml && bundle exec rake db:migrate db:seed"
  },
  "env": {
    "GITHUB_CLIENT_ID": {
      "required": true
    },
    "GITHUB_CLIENT_SECRET": {
      "required": true
    },
    "LANG": {
      "required": true
    },
    "RACK_ENV": {
      "required": true
    },
    "RAILS_ENV": {
      "required": true
    }
  },
  "addons": [
    "heroku-postgresql"
  ],
  "buildpacks": [

  ]
}
